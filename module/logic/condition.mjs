/** @import DegenesisActor from '../documents/actor.mjs'  */

/**
 * Class containing Degenesis system logic for preparing and calculating data,
 * helper functions mostly.
 */

export default class Condition {
  /**
   * Calculating maximum trauma.
   * @param {DegenesisActor} actor reference to actor
   */
  static calculateMaxTrauma(actor) {}
  static calculateMaxFleshwounds(actor) {}
  static calculateMaxEgo(actor) {}
  static calculateMaxSpore(actor) {}
  static calculateMovement(actor) {}
}
